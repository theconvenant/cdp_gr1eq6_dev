<!DOCTYPE html>
<html lang="en">

<head>
    <% include  ./includes/header %>
    <script type="text/javascript">
        changeOnglet('issues')
    </script>
</head>
            
<body>
    </div>
    <% if (issueList.length != 0) { %>
    <div id="IssueList" class="w3-margin">
        <table class="w3-table-all w3-hoverable w3-card-4">
            <h3 class="w3-margin w3-center">Issues List</h3>
            <tr class="w3-blue">
                <th>Issue id</th>
                <th>Description</th>
                <th>Difficulty</th>
                <th>Priority</th>
                <th>test_state</th>
            </tr>
            <% for (var i = 0; i < issueList.length; ++i) { %>
            <tr class="Projects">
                <td><%= issueList[i]._issue_id %></td>
                <td><%= issueList[i].description %></td>
                <td><%= issueList[i].difficulty %></td>
                <td><%= issueList[i].priority %> </td>
                <td><%= issueList[i].test_state %> </td>
                <th><button onclick="document.getElementById('updateIssue<%=issueList[i]._issue_id %>').style.display='block'"
                    class="w3-btn w3-ripple w3-green">Modify</button></th>

                    <div class="w3-margin w3-center">
                        <div id="updateIssue<%=issueList[i]._issue_id %>" class="w3-modal">
                            <div class="w3-modal-content">
                                <div class="w3-container">
                                    <form class="w3-container" action="/updateIssue" method="POST">
                                        <label>Issue id</label>
                                        <input class="w3-input w3-border w3-round" type="number" name="issueId" value="<%=issueList[i]._issue_id %>" readonly>
                                            
                                        <label>Description</label>
                                        <input class="w3-input w3-border w3-round" type="text" name="description" value="<%=issueList[i].description %>">
                
                                        <label>Difficulty</label>
                                        <input class="w3-input w3-border w3-round" type="number" name="difficulty" value="<%=issueList[i].difficulty %>">
                
                                        <label>Priority</label>
                                        <input class="w3-input w3-border w3-round" type="number" name="priority" value="<%=issueList[i].priority %>">

                                        <label>State</label>
                                        <input class="w3-input w3-border w3-round" type="text" name="state" value="<%=taskList[i].test_state %>">
                
                                        <input class="w3-btn w3-margin-top w3-ripple w3-green" type="submit" value="Validate">
                                        
                                    </form>
                                    <button onclick="document.getElementById('updateIssue<%=issueList[i]._issue_id %>').style.display='none'"
                                        class="w3-btn w3-ripple w3-margin-top w3-red">Cancel</span>
                                </div>
                            </div>
                        </div>
                    </div> 
                    <th>
                        <form action="/deleteIssue" method = "post">
                            <button type="submit" value='<%=issueList[i]._issue_id %>'
                                name="issueId" class="w3-btn w3-ripple w3-red">Remove</button>    
                        </form>
                    </th>
                </tr>
            <% }; %>
        </table>
    </div>
    <% }; %>
    <div class="w3-margin w3-center">

        <button onclick="document.getElementById('addIssue').style.display='block'"
        class="w3-btn w3-ripple w3-green">+ Add Issue</button>

        <div id="addIssue" class="w3-modal">
            <div class="w3-modal-content">
                <div class="w3-container">
                    <form class="w3-container" action="/Issue" method="POST">
                        <label>Issue id</label>
                        <input class="w3-input w3-border w3-round" type="number" name="issueId" placeholder="must be an integer" required>
                            
                        <label>Description</label>
                        <input class="w3-input w3-border w3-round" type="text" name="description" required>

                        <label>Difficulty</label>
                        <input class="w3-input w3-border w3-round" type="number" name="difficulty" placeholder="must be an integer">

                        <label>Priority</label>
                        <input class="w3-input w3-border w3-round" type="number" name="priority">

                        <label>State</label>
                        <input class="w3-input w3-border w3-round" type="text" name="state" placeholder="TODO or ...">

                        <input class="w3-btn w3-margin-top w3-ripple w3-green" type="submit" value="Validate">

                        <button onclick="document.getElementById('addIssue').style.display='none'"
                    class="w3-btn w3-ripple w3-margin-top w3-red">Cancel</span>
                    </form>
                </div>
            </div>
        </div>
    </div>


</body>
</html>
