language: node_js
node_js:
  - 10

dist: xenial

services:
  - xvfb

addons:
  chrome: stable

before_install:
  - docker-compose build
  - docker-compose up -d

before_script:
  - travis_wait 1 sleep 20

script:
- "cd ./code"
- "npm install"
- "npm test"