dist: trusty
addons:
  apt:
    sources:
      - google-chrome
    packages:
      - google-chrome-stable

language: node_js

node_js:
  - 10

before_install:
  - docker-compose build
  - docker-compose up -d

before_script:
  - travis_wait 1 sleep 60

script:
  - "npm test"